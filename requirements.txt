# RAG 医疗问答系统依赖
# 注意:整套 langchain 生态必须统一走 1.0+ 路线
#       (langchain-classic 1.0.x 要求 langchain-core>=1.3.3)
# 推荐 Python 3.10+

# --- 核心 langchain 生态(全部 1.0+) ---
langchain>=1.0,<2.0
langchain-core>=1.3.3,<2.0
langchain-classic>=1.0,<2.0            # 旧 API 兼容包,代码里 RetrievalQA 在这里
langchain-huggingface>=0.3,<2.0        # HuggingFaceEmbeddings
langchain-ollama>=0.3,<2.0             # OllamaLLM
langchain-text-splitters>=0.3,<2.0

# --- 向量库 / 检索 ---
chromadb>=0.5,<1.0
rank_bm25>=0.2.2                       # BM25Retriever 底层依赖
jieba>=0.42                            # 中文分词,用于 BM25 索引

# --- Embedding / 切分 ---
sentence-transformers>=3.0
transformers>=4.46
torch>=2.0.0

# --- XML 解析(P0 改造后需要) ---
beautifulsoup4>=4.12
lxml>=5.0

# --- 工具 ---
huggingface_hub
tiktoken
matplotlib                            # 给 data_analysis.py 用
numpy
